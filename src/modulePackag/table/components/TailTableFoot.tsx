import { defineComponent, VNodeChild, RenderFunction, h } from 'vue'

export default defineComponent({
  name: 'TailTableFoot',
  inheritAttrs: false, // props: {},
  // emits: {},
  setup(props, { attrs, emit, expose, slots }): RenderFunction {
    return (): VNodeChild => {
      //
    }
  }
})
